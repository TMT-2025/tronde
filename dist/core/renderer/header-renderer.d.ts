import { TemplateProfile } from "./template-loader.js";
export declare function renderExamTitle(title: string): string;
export declare function renderStudentHeaderTable(examCode: string, profile: TemplateProfile): string;
